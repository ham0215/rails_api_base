name: push piyo ci

on:
  push:
    paths:
      - 'piyo/**'

jobs:
  piyo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.4
      - name: run piyo
        run: piyo/main.sh
